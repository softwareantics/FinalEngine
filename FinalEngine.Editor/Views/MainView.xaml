<mah:MetroWindow x:Class="FinalEngine.Editor.Views.MainView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:views="clr-namespace:FinalEngine.Editor.Views"
                 mc:Ignorable="d"
                 Width="800"
                 Height="450"
                 WindowStartupLocation="CenterScreen"
                 WindowState="Maximized"
                 GlowBrush="{DynamicResource MahApps.Brushes.Accent2}">

    <mah:MetroWindow.LeftWindowCommands>
        <mah:WindowCommands ShowLastSeparator="False">
            <views:MainMenuView DataContext="{Binding MainMenuViewModel}"/>
        </mah:WindowCommands>
    </mah:MetroWindow.LeftWindowCommands>

    <Grid>
        <views:DockView DataContext="{Binding DockViewModel}"/>
    </Grid>
</mah:MetroWindow>